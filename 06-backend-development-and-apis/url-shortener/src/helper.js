const saveUrl = async (originalUrl) => {
  const shortUrl = "";
  return shortUrl;
};

const fetchUrl = (shortUrl) => {
  const originalUrl = "";
  return originalUrl;
};

module.exports = { saveUrl, fetchUrl };
